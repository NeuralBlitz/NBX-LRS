# NeuralBlitz v50 Production Dependencies
# Updated for Security Compliance - Feb 2026

# --- Web Framework ---
# Bumped Flask to ensure underlying Werkzeug security fixes
flask>=3.0.3
# Fixed: Log injection & Regex DoS. Note: v6.0.0 does not exist on PyPI, v5.0.0 is current stable.
Flask-Cors>=5.0.0
flask-sock>=0.7.0
flask-socketio>=5.3.6
# Fixed: Request Smuggling (CVE-2022-42969 / CVE-2024-XXXX)
gunicorn>=22.0.0
gevent>=24.2.1
gevent-websocket>=0.10.1

# --- Scientific Computing ---
numpy>=1.26.4
scipy>=1.13.0

# --- Quantum Computing (optional) ---
# qiskit>=1.0.0
# qiskit-aer>=0.13.0

# --- Data Processing ---
pandas>=2.2.0

# --- Async Support & WebSockets ---
# CRITICAL FIX: aiohttp bumped to fix Directory Traversal, Zip Bomb, and multiple DoS vectors.
aiohttp>=3.10.5
# NOTE: 'asyncio' package removed. It is built-in for Python 3.7+. Installing the PyPI package causes conflicts.
websockets>=12.0
# Fixed: Pickle Deserialization RCE
python-socketio>=5.11.3
python-engineio>=4.9.1
# Fixed: Tudoor Mechanism DoS & Request Smuggling
eventlet>=0.36.1

# --- Production & Monitoring ---
prometheus-client>=0.20.0

# --- Security ---
# Fixed: Bleichenbacher timing attack, Null pointer deref, & OpenSSL wheel issues.
cryptography>=43.0.1
pyOpenSSL>=24.0.0
PyJWT>=2.9.0

# --- Utilities ---
python-dotenv>=1.0.1
pyyaml>=6.0.2

# --- Testing ---
pytest>=8.0.0
pytest-cov>=4.1.0
# Fixed: .netrc credential leak and verify=False session persistence
requests>=2.32.3
